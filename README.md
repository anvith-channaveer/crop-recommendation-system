# crop-recommendation-system
A machine learning–based crop recommendation system that suggests the most suitable crop based on soil and environmental parameters. The frontend is developed using HTML, CSS, and JavaScript, while the backend is implemented in Python. The ML model is trained using agricultural datasets to help farmers make data-driven crop selection decisions
